# Capture form

Create one Task for one finish. Create a Project only for one outcome that needs
more than one Task. A new Project may include its known Tasks in `tasks`.

Use a named exact Area or Project destination only when the owner supplied it.
Omit every field the owner did not set. The server preserves omitted fields.
