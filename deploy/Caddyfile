# Reference only. `things-orchestrator print-config --client caddy` renders the
# configured hostname. Install the result at /etc/caddy/Caddyfile and run Caddy
# through its systemd service. See docs/install.md.

mcp.example.com {
	reverse_proxy 127.0.0.1:8787 {
		flush_interval -1
	}
}
